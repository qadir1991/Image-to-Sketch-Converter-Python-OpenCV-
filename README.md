# Image-to-Sketch-Converter-Python-OpenCV-
This is a Python-based image processing application that allows users to upload an image and convert it into a pencil sketch.  The program uses a file picker to let the user select an image, processes it using OpenCV, and generates a sketch-style output.
